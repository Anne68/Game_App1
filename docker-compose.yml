services:
  api:
    build: .
    env_file: .env
    ports: ["8080:8080"]
    depends_on: ["db"]
  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: games
      MYSQL_ROOT_PASSWORD: root
    ports: ["3306:3306"]
