name: Production Monitoring

on:
  schedule:
    # Toutes les 6 heures
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      api_url:
        description: "URL de l'API à monitorer"
        required: false
        default: 'https://game-app-y8be.onrender.com'
      alert_level:
        description: "Niveau d'alerte"
        required: false
        default: 'warning'
        type: choice
        options: [info, warning, critical]

concurrency:
  group: production-monitoring
  cancel-in-progress: true

jobs:
  health-monitoring:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Valeur par défaut si l'entrée du workflow est vide
      API_URL: ${{ github.event.inputs.api_url != '' && github.event.inputs.api_url || 'https://game-app-y8be.onrender.com' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests python-dotenv

      # ---- WARM-UP & HEALTH CHECK (curl) ----
      # On tente /healthz puis / avec retries + backoff pour réveiller Render.
      - name: Warm up & Health check (curl with retries)
        shell: bash
        run: |
          set -e
          URL="${API_URL%/}"
          echo "🏥 Vérification (curl) de: $URL"

          try_once () {
            local path="$1"
            # accepte 2xx/3xx/4xx (service joignable), timeout 25s
            CODE=$(curl -s -o /dev/null -w "%{http_code}" \
              --connect-timeout 10 \
              --max-time 25 \
              --retry 2 \
              --retry-all-errors \
              "$URL$path" || true)
            echo "  -> $path => HTTP ${CODE:-none}"
            if [[ "$CODE" =~ ^[234][0-9]{2}$ ]]; then
              return 0
            fi
            return 1
          }

          for i in {1..8}; do
            # on tente d'abord /healthz puis /
            if try_once "/healthz" || try_once "/"; then
              echo "✅ Service joignable."
              exit 0
            fi
            SLEEP=$(( 5 + i*5 ))
            echo "⏳ Nouvelle tentative dans ${SLEEP}s…"
            sleep "$SLEEP"
          done

          echo "❌ Le service n'a pas répondu à temps (curl)."
          exit 1

      # ---- Fallback Python plus verbeux (ne s'exécute que si l'étape précédente échoue) ----
      - name: API Health Check (Python fallback)
        if: ${{ failure() }}
        run: |
          python - <<'PY'
          import os, time, requests, sys
          url = (os.getenv("API_URL") or "https://game-app-y8be.onrender.com").rstrip("/")
          print(f"🏥 Vérification (Python) de: {url}")

          paths = ["/healthz", "/"]
          for attempt in range(1, 7):
              for path in paths:
                  try:
                      r = requests.get(url + path, timeout=25)
                      print(f"Attempt {attempt} {path}: {r.status_code}")
                      if 200 <= r.status_code < 500:
                          # si /healthz renvoie du JSON avec des clés de statut, on vérifie
                          if path == "/healthz":
                              try:
                                  data = r.json()
                                  status = data.get("status", "unknown")
                                  model_loaded = data.get("model_loaded", True)
                                  db_ready = data.get("db_ready", True)
                                  print(f"Status={status}, model_loaded={model_loaded}, db_ready={db_ready}")
                                  if status == "healthy" and model_loaded and db_ready:
                                      print("🎉 Health check passed")
                                      sys.exit(0)
                                  else:
                                      print("⚠️ /healthz OK mais statut applicatif non healthy.")
                              except Exception as e:
                                  print(f"ℹ️ /healthz non-JSON ou parsing raté: {e}")
                          else:
                              print("✅ Service joignable (même si /healthz indisponible).")
                              sys.exit(0)
                  except Exception as e:
                      print(f"Attempt {attempt} {path} error: {e}")
              time.sleep(15 * attempt)

          print("💥 Health check failed after retries.")
          sys.exit(1)
          PY

  security-monitoring:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      API_URL: ${{ github.event.inputs.api_url != '' && github.event.inputs.api_url || 'https://game-app-y8be.onrender.com' }}

    steps:
      - name: Check SSL/TLS
        shell: bash
        run: |
          URL="${API_URL%/}"
          HOSTNAME=$(echo "$URL" | sed 's|https\?://||' | cut -d'/' -f1)
          echo "🔒 Checking SSL for: $HOSTNAME"

          if command -v openssl >/dev/null 2>&1; then
            echo | openssl s_client -servername "$HOSTNAME" -connect "$HOSTNAME:443" -brief 2>/dev/null | head -20 || true
            EXPIRY=$(echo | openssl s_client -servername "$HOSTNAME" -connect "$HOSTNAME:443" 2>/dev/null | openssl x509 -noout -dates | grep notAfter | cut -d= -f2)
            if [ -n "$EXPIRY" ]; then
              echo "📅 Certificate expires: $EXPIRY"
            fi
          else
            echo "⚠️ OpenSSL not available for SSL check"
          fi

      - name: Check API Security Headers
        run: |
          python - <<'PY'
          import os, requests
          url = (os.getenv("API_URL") or "https://game-app-y8be.onrender.com").rstrip("/")
          try:
              r = requests.get(url + "/healthz", timeout=15)
              headers = r.headers
              security_headers = [
                  "X-Content-Type-Options",
                  "X-Frame-Options",
                  "X-XSS-Protection",
                  "Strict-Transport-Security",
              ]
              print("🛡️ Security Headers Check:")
              for h in security_headers:
                  if h in headers:
                      print(f"  ✅ {h}: {headers[h]}")
                  else:
                      print(f"  ⚠️ {h}: Missing")
          except Exception as e:
              print(f"⚠️ Security headers check error: {e}")
